# 复制为 .env / .env.local 覆盖本地值；开发默认见 .env.development，构建默认见 .env.production
# 仅 VITE_ 前缀会注入前端（见 https://vite.dev/guide/env-and-mode.html）

# API 根路径（axios baseURL，需与后端路由前缀一致）
VITE_API_BASE_URL=/api/v1

# HTTP 超时（毫秒，正整数）；留空则开发 120000、生产 8000
VITE_HTTP_TIMEOUT_MS=
