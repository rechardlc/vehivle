# Go 后端学习记录（2026-03-21）

> 今日学习内容，知识库见 [lesson-20260317.md](./lesson-20260317.md)，进度见 [progress.md](./progress.md)。昨日见 [lesson-20260320.md](./lesson-20260320.md)。

| 元数据 | 值 |
|--------|-----|
| **学习类型** | 实战（数据库迁移与本地依赖）+ 架构理解（分层链路、Go 接口哲学） |
| **对应阶段** | [循序渐进](../循序渐进总说明.md) 第 4 步（数据库与迁移）/ [learning-path](../learning-path/go-backend-learning-roadmap.md) 阶段 3～5 |
| **涉及目录** | `migrations/`、`cmd/migrate`、`deploy/docker`、`Makefile`、`internal/service/vehicle`、`internal/repository/postgres`、`internal/transport/http/handler` |

---

## 今日目标

- 理解「连接」与「迁移」分工：DSN 只解决连通性，表结构由版本化 SQL 落地。
- 本地 PostgreSQL/Redis 可重复启动；迁移可一键 `up`，且 Windows 可执行。
- 理清 Handler → Service → Repository → DB 全链路，识别断裂点并修复（**车型 `List` 已落地**）。
- 理解 Go 接口哲学与依赖注入方式，对比前端（TypeScript）思维差异。

---

## 一、数据库迁移与本地依赖（上午）

### 完成

1. **本地依赖编排**：`deploy/docker/docker-compose.yml`
   - PostgreSQL、Redis；`pull_policy: never` 仅用本机已有镜像；默认 `postgres:13`、`redis:latest`（与常见本机环境一致）；`deploy/docker/.env.example` 可覆盖 `POSTGRES_IMAGE` / `REDIS_IMAGE`。
   - `deploy/docker/README.md`：启动/停止、与 `VEHIVLE_DATABASE_DSN` / `VEHIVLE_REDIS_ADDR` 对齐说明。

2. **golang-migrate 与命令入口**
   - `cmd/migrate/main.go`：`configs.Load()` 读 DSN；`up` / `down` / `version`；`migrate.NewWithSourceInstance` + **`iofs` + `os.DirFS(migrations)`**（避免 Windows 下 `file://` 解析失败、`open .` 报错）。
   - `Makefile`：`migrate-up`、`migrate-down`、`migrate-version`（与 `VEHIVLE_APP_ENV`、`make run` 同目录约定）。

3. **首版迁移 SQL**：`migrations/000001_init_schema.up.sql` / `.down.sql`
   - 表：`admin_users`、`vehicles`、`system_settings`；CHECK 与 domain 枚举对齐。
   - 每张表、每个字段：`--` 行注释 + `COMMENT ON TABLE` / `COMMENT ON COLUMN`（便于 `\d+` 与协作）。

4. **文档**：`migrations/README.md` 补充字段注释约定、已执行迁移勿改、补注释用新编号迁移。

### 核心概念

| 概念 | 要点 |
|------|------|
| **up / down** | `up` 应用结构变更；`down` 回滚一步（开发环境慎用）。 |
| **版本号** | `000001`、`000002` 递增；已执行过的 `.sql` 不改，新变更用新文件。 |
| **`schema_migrations`** | 由工具维护，记录当前版本，避免重复执行。 |
| **iofs** | 迁移文件用 `fs.FS` 提供，跨平台不依赖 `file://` URL。 |

---

## 二、分层链路分析与修复（下午）

### 2.1 全链路诊断

对 `server/` 全链路做了逐层检查：`main → Bootstrap → Router → Handler → Service → Repository → DB`。

#### 各层现状（截至本课记录定稿）

| 层 | 状态 | 说明 |
|---|---|---|
| main → Bootstrap → Router | ✅ 通畅 | 配置加载、DB 初始化、中间件注册、路由挂载 |
| Domain（enum / model / rule） | ✅ 通畅 | `Vehicle` 模型、状态枚举、发布规则可用 |
| Infrastructure（Postgres 连接） | ✅ 通畅 | Open / Ping / Close 可用 |
| **Handler → Service（车型）** | ✅ 已修复 | `NewVehicles` 内 `NewVehicleRepo(db)` → `NewService(repo)` |
| **Service** | ✅ | `VehicleRepo` 接口在 service 包；`Publish`、`List` 可用 |
| **Repository 实现** | ✅ 部分 | `GetById`、`Update`、`List` 已实现；`Create`/`Delete` 待补 |
| **Handler 方法体** | ⏳ | `List` 调 Service 并返回 `items`；`Create`/`Update`/`Delete` 仍为占位 |

#### 已修复问题（历史断裂点）

| # | 原问题 | 处理方式 |
|---|--------|----------|
| 1 | 曾误将 `*gorm.DB` 传入 `NewService` | `NewVehicleRepo(db)` → `NewService(repo)` |
| 2 | `VehicleRepo` 缺 `Update` | `Save(vehicle)` 实现 `Update`，供 `Publish` 使用 |
| 3 | `List` 未走业务层 | `Service.List` + `Repo.List`；公开路径 `onlyPublished=true` 仅 `published` |

#### 依赖注入链（当前实现）

`Router` 仍传入 `*gorm.DB`，由 **`NewVehicles(db)` 内部** 组装 `repo → service`（与「router 只认识 db」的写法一致）：

```
Router（*gorm.DB）
  └─ NewVehicles(db)
       └─ postgres.NewVehicleRepo(db)
       └─ vehicle.NewService(repo)
            └─ List: handler → Service.List → VehicleRepo.List → GORM → PostgreSQL
```

### 2.2 Go 接口哲学（核心认知转变）

#### "结构体" vs "结构"（中文语境）

| 说法 | 指什么 | Go 关键字 | 示例 |
|------|--------|-----------|------|
| **结构体** | 有固定字段的数据类型 | `struct` | `type Service struct { vehicles VehicleRepo }` |
| **接口** | 一组方法签名的约定 | `interface` | `type VehicleRepo interface { GetById(...) }` |
| **结构**（口语） | 架构、组织、层次 | 无 | "项目结构""分层结构" |

#### Go 接口 vs TypeScript 接口：定义位置不同

| 维度 | TypeScript / Java | Go |
|------|-------------------|-----|
| 接口定义在哪 | **提供方**（如 `repository/`） | **消费方**（如 `service/vehicle/`） |
| 实现方式 | `class Foo implements IBar` 显式声明 | **隐式**：方法签名匹配即满足（鸭子类型） |
| 核心思想 | "我告诉你我长什么样" | **"我只关心你能做什么"** |

**Go 标准库的经典示例**：`io.Reader` 定义在 `io` 包（使用方），`os.File` 从未写 `implements io.Reader`，但它方法匹配，自动就是 `io.Reader`。

#### 本项目的正确做法

```go
// service/vehicle/service.go —— 消费方定义接口
type VehicleRepo interface {
    GetById(ctx context.Context, id string) (*model.Vehicle, error)
    Update(ctx context.Context, v *model.Vehicle) error
}

// repository/postgres/vehicle_repo.go —— 实现方只管实现方法
// 不需要 import service 包，不需要写 implements
func (v *VehicleRepo) GetById(...) (*model.Vehicle, error) { ... }
func (v *VehicleRepo) Update(...) error { ... }
```

**依赖方向**：`service` 包不 import `repository/postgres`；`VehicleRepo` 由 postgres 侧**隐式实现**。装配可在 **handler 构造函数**（当前做法）或后续集中到 `bootstrap`/`wire`。

### 2.3 service.go 的四件套模式

当前 `service/vehicle/service.go` 的代码结构是 Go 后端常见的四件套：

```go
// 1. 接口（声明需要的能力）
type VehicleRepo interface { ... }

// 2. 结构体（持有依赖）
type Service struct { vehicles VehicleRepo }

// 3. 构造函数（注入依赖，返回指针）
func NewService(vehicles VehicleRepo) *Service { ... }

// 4. 业务方法（指针接收者，操作依赖）
func (s *Service) Publish(ctx context.Context, ...) error { ... }
```

| 组件 | 作用 | 前端类比 |
|------|------|----------|
| `interface` | 声明依赖契约 | TypeScript 的 `interface`，但定义在使用方 |
| `struct` | 存储状态与依赖 | 类的实例字段 / React 组件的 state |
| `NewService()` | 工厂函数 | `new Service()` 或 `createService()` |
| `(s *Service) Method()` | 指针接收者方法 | 类的方法 `this.method()` |

---

## 三、当前全链路状态（车型）

```
main ✅
  └─ Bootstrap ✅ (cfg → logger → DB → middleware → router)
       └─ Router ✅ (admin/public 分组, health, 404/405)
            └─ Handler ✅ List（Create/Update/Delete ⏳ 占位）
                 └─ Service ✅（Publish、List）
                      └─ Repository ✅ GetById、Update、List（Create/Delete ⏳）
                           └─ *gorm.DB ✅
```

---

## 下一步（对齐文档）

1. **Repository / Service**：`Create`、`Delete`（及分页、筛选），与 [循序渐进第 4 步](../循序渐进总说明.md)「基础增删改查」收尾。
2. **Handler**：`Create`/`Update`/`Delete` 接 Service，替换占位响应。
3. **认证**：[循序渐进第 5 步](../循序渐进总说明.md) / [learning-path 阶段 4](../learning-path/go-backend-learning-roadmap.md) 登录与 RBAC。
4. **User 域**：补 Service + Repository，或合并到统一「账户」迭代。

---

## 复盘

- **上午**：「连上库」≠「有表」；`make migrate-up` 后才有真实 schema 与 `schema_migrations`。Windows 下迁移源优先 **iofs**。
- **下午**：全链路诊断后已落地 **List**：`NewVehicleRepo` + `NewService`，`Repo` 实现 `Update`/`List`，公开列表仅 `status = published`。
- **认知转变**：Go 接口由消费方定义（`service` 定义 `VehicleRepo`），实现方（`repository/postgres`）隐式满足，不需要 `implements` 关键字。
- **四件套模式**（interface → struct → NewXxx → method）是 Go 服务层的标准写法，后续所有 service 都可套用。
- `internal/repository/` 目录下不需要单独定义接口文件，接口已归属到各 service 包中。
