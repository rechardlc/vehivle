# configs

## 作用
管理不同环境（dev/test/prod）的配置模板与加载规则。

## 推荐内容
- 应用配置：端口、超时、日志级别。
- 数据库配置：PostgreSQL DSN、连接池参数。
- 缓存配置：Redis 地址、超时。
- 对象存储配置：Bucket、Region、签名有效期。
- 安全配置：JWT 密钥、过期时间、密码策略。

## 实践建议
- 敏感值走环境变量，不写死在仓库。
- 配置项统一结构化，避免散落常量。
- 每新增一项配置，都同步补默认值和校验规则。
